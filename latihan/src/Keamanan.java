public interface Keamanan {
    boolean autentikasi();
}

// Interface yang berisi method autentikasi() untuk memastikan setiap transaksi memiliki proses verifikasi. Method ini mengembalikan nilai boolean //